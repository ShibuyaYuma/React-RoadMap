/*
react spring: アニメーションライブラリ. 物理ベースのアニメーションを簡単に実装できる

インストール: npm install @react-spring/web
*/

import { useSpring, animated } from "@react-spring/web";

// const ReactSpring: React.FC = () => {
//     const styles = useSpring({
//         from: { opacity: 0 },
//         to: { opacity: 1 },
//         config: { tension: 200, friction: 20 }, // 物理ベースの設定
//     });

//     return <animated.div style={styles}>Hello, React Spring!</animated.div>;
// };

//export default ReactSpring;

export {};
